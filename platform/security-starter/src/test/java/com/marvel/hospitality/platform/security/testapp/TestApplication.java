package com.marvel.hospitality.platform.security.testapp;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Minimal service for the starter's tests; the starter is picked up through its AutoConfiguration.imports exactly as
 * in a real service. In a sub-package so component scanning cannot register the starter's controller a second time.
 */
@SpringBootApplication
public class TestApplication {
}
